name: CI
on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - name: setup AWS
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: AKIAZKDERARPAWXESDOB
          aws-secret-access-key: 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
          aws-region: us-east-1
      - name: set $COMMIT_MSG
        run: echo "COMMIT_MSG=${{ github.event.head_commit.message || github.event.pull_request.title }}" >> $GITHUB_ENV
